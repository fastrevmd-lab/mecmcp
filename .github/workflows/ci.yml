name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings
  RUSTDOCFLAGS: -D warnings

jobs:
  build-test:
    name: Build, lint, and test
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Build workspace
        run: cargo build --workspace

      - name: Run tests
        # `test-util` on both crates, or the tests behind it are silently not
        # run. They are gated because they reach doors the public API does not
        # offer -- seeding a change set in a state only the lifecycle can
        # produce, and installing a tracing subscriber per thread -- and a gate
        # that CI does not open is a test that does not exist.
        run: cargo test --workspace --features mecmcp-audit/test-util,mecmcp-changeset/test-util

      - name: Build documentation
        run: cargo doc --workspace --no-deps

  msrv:
    # mecmcp-auth is a library consumed by two binaries, so an accidental MSRV
    # bump breaks them rather than this repo. That makes it exactly the wrong
    # thing to discover downstream.
    name: Minimum supported Rust 1.88
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # rust-toolchain.toml pins the working toolchain; the MSRV floor declared
      # in Cargo.toml is deliberately lower. Remove the pin so the 1.88
      # toolchain below is what actually runs.
      - name: Drop the toolchain pin
        run: rm -f rust-toolchain.toml

      - name: Install Rust 1.88
        uses: dtolnay/rust-toolchain@1.88.0

      # `Cargo.lock` is gitignored here, so this job resolves fresh on every run
      # and takes whatever the registry currently offers. That is what makes it
      # useful -- it catches a dependency raising its floor -- and also what
      # lets an unrelated release turn every open PR red with no commit behind
      # it. `aes` 0.9.3 declared `rust-version = 1.89`, reached through
      # russh -> ssh-key -> aes-gcm -> aes, which made our 1.88 unsatisfiable.
      #
      # Pinning the last 1.88-compatible release keeps this job measuring *our*
      # MSRV rather than the registry's. When a pin here is no longer needed,
      # drop it; when a new one is needed, the failure names the crate and the
      # version it wants, which is the whole diagnosis.
      - name: Pin dependencies that dropped 1.88 support
        run: cargo update -p aes --precise 0.9.2

      - name: Check workspace at MSRV
        run: cargo check --workspace --all-targets
