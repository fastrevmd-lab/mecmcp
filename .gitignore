/target
**/*.rs.bk
Cargo.lock

# Agent scratch: git worktrees for parallel task branches.
/.claude/
/.worktrees/

# Never commit real authentication material, even hashed.
tokens.json
*.tokens.json
!crates/*/tests/fixtures/*-tokens.json

.DS_Store
