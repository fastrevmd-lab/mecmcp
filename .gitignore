/target
**/*.rs.bk
Cargo.lock

# Agent scratch: git worktrees for parallel task branches.
/.claude/
/.worktrees/

# Never commit real authentication material, even hashed.
tokens.json
*.tokens.json
!crates/*/tests/fixtures/*-tokens.json

.DS_Store
tem_todo
docs/codex.thoughts
# Local-only working documents. Untracked on purpose — keep them out of any
# `git add -A`, and do not overwrite them.
docs/ARCHITECTURE.md
docs/ONBOARDING.md
