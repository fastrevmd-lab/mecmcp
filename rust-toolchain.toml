# Pin the toolchain both consumers already use. MSRV 1.89 is the compatibility
# floor declared in Cargo.toml, not the toolchain we build with.
[toolchain]
channel    = "1.98.0"
profile    = "minimal"
components = ["rustfmt", "clippy"]
